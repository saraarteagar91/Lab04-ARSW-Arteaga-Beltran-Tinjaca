# AI Usage Declaration

Declaring AI use does not reduce the grade. You must be able to explain and validate every submitted decision.

| Tool | Activity | Prompt / purpose | How I validated the result | What I changed / rejected |
|---|---|---|---|---|
| Claude Code (Sonnet 5) | Implement `BoardApplicationService` use cases (create/get/replace) | Asked to complete the `TODO LAB-04` methods following the port already defined by `BoardRepository`, keeping id generation server-side and using `BoardNotFoundException` for missing boards | Ran `mvn test` and manually re-read each method against the lab's RA-01..RA-07 requirements before accepting | None rejected; kept the generated code as-is since it matched the required DIP structure |
| Claude Code (Sonnet 5) | Implement `InMemoryBoardRepository` (upsert semantics) and finish the `BoardRepository` port javadoc | Asked to decide and document save/replace semantics per the existing TODO comments | Verified with unit tests that create + replace both work through the same `save` method and that replace preserves the original id | Removed a suggestion to add defensive copying, since `Board`/`BoardElement` are already immutable records — unnecessary |
| Claude Code (Sonnet 5) | Add a catch-all `Exception` handler in `GlobalExceptionHandler` | Asked how to guarantee no stack trace or internal Java message ever reaches the client, per RA-05 | Confirmed the handler returns a fixed generic message (`"Unexpected server error"`), not `ex.getMessage()` | Accepted as proposed |
| Claude Code (Sonnet 5) | Write unit tests (`BoardApplicationServiceTest`) and MockMvc REST tests (`BoardRestControllerTest`) | Asked to enable the disabled test, add a replace-flow test, a not-found-on-replace test, and HTTP-level tests covering create/get/replace and the 404/400 error contract | Executed `mvn test` locally; all 7 tests pass (`BUILD SUCCESS`) | None rejected |
| Claude Code (Sonnet 5) | Draft `docs/api-contract.md` and `docs/ADR-001-repository-boundary.md` | Asked to document the actual implemented contract and the repository-boundary decision with the required ADR sections | Cross-checked every field/status code against the real controller and exception handler code, not from memory | Adjusted the ADR's "Trade-off" section to be honest about the added indirection instead of only listing benefits |

| Claude Code (Sonnet 5) | Add unit tests for `Board` and `BoardElement` invariants | Asked for direct coverage of the compact-constructor invariants (required id/name, null-elements defaulting, defensive copying, negative width/height, null-text default) that were previously only exercised indirectly | Ran `mvn test`; all new tests pass without changing any production code | None rejected |
| Claude Code (Sonnet 5) | Add unit tests for `InMemoryBoardRepository` | Asked for direct adapter coverage (save/findById, missing-id case, existsById, upsert-on-existing-id) since it was previously only exercised indirectly through `BoardApplicationServiceTest` | Ran `mvn test`; verified each assertion against the adapter's actual `save`/`findById`/`existsById` behavior | None rejected |
| Claude Code (Sonnet 5) | Add unit tests for `BoardNotFoundException` and unique id generation in `BoardApplicationServiceTest` | Asked to cover the exception's message format and `boardId()` accessor, and to assert `createBoard` produces distinct ids per call | Ran `mvn test`; confirmed both new assertions pass | None rejected |
| Claude Code (Sonnet 5) | Fix a bug found while writing REST tests: a `PUT` with an invalid nested `BoardElement` (e.g. negative width) returned `500 INTERNAL_ERROR` instead of the documented `400 INVALID_INPUT` | Asked to explain why, since `IllegalArgumentException` from the record's compact constructor is thrown *during* Jackson deserialization and gets wrapped in `HttpMessageNotReadableException` before Spring's existing `IllegalArgumentException` handler ever runs | Reproduced the 500 with a MockMvc test first, added a `HttpMessageNotReadableException` handler that unwraps the cause chain, then confirmed the same test now gets `400 INVALID_INPUT`; ran `mvn test` and manually verified via `curl` against the running app | Kept the fix minimal — one new `@ExceptionHandler` method following the existing pattern, no change to `RA-01..RA-07` boundaries |
| Claude Code (Sonnet 5) | Add `scripts/demo.ps1` and `scripts/demo.sh`, and document them in the README | Asked for a copy-pasteable way to demo the API end to end (create/read/replace/404/400) after discovering that PowerShell's native-command argument parsing mangles `curl -d "{...}"` with escaped quotes | Ran both scripts against the locally running app and confirmed the printed output matches the documented contract in `docs/api-contract.md` | None rejected |

No architecture, package structure, or domain model was invented by the AI beyond what the starter/lab guide specified — the AI completed the marked `TODO LAB-04` items, added test coverage, fixed one error-handling gap the new tests surfaced, and added documentation/demo tooling, all within the existing structure.
